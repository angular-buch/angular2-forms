/*
import {
  it as angular_it,
  describe as angular_describe,
  expect as angular_expect
} from 'angular2/testing';

describe("angular2/testing offers the same jasmine functions", () => {

  // see testing.ts -- var jsmIt = _global.it;
  it("'it' is in fact just _global.it", () => {
    expect(it === angular_it).toBe(true);
  });

  // see testing.ts -- export var describe: Function = _global.describe;
  it("'describe' is in fact just _global.describe", () => {
    expect(describe === angular_describe).toBe(true);
  });

  // see matchers.ts -- export var expect: (actual: any) => NgMatchers = <any>_global.expect;
  it("'expect' is in fact just _global.expect", () => {
    expect(expect === angular_expect).toBe(false);
  });
});
*/  
